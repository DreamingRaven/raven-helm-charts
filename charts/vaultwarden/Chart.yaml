apiVersion: v2
appVersion: 1.36.0 # renovate: datasource=docker depName=ghcr.io/felddy/foundryvtt
description: Unofficial Bitwarden compatible server written in Rust
name: vaultwarden
type: application
version: 0.1.0
dependencies:
- name: corvid-app
  alias: vaultwarden
  version: 0.17.0
  repository: "oci://registry.gitlab.com/georgeraven/raven-helm-charts"
  condition: vaultwarden.enabled
